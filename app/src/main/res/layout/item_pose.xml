<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="8dp">


    <!-- item_pose.xml (inside the pose item root, likely a FrameLayout/ConstraintLayout) -->
    <ImageView
        android:id="@+id/nsfwBadge"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="top|end"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:padding="2dp"
        android:src="@drawable/ic_warning_small"
        android:visibility="gone"/>

    <ImageButton
        android:id="@+id/poseImg"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:scaleType="centerInside"
        android:src="@drawable/placeholder_avatar"
        android:contentDescription="@string/pose_placeholder"
        android:layout_marginBottom="4dp"/>

    <EditText
        android:id="@+id/poseLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/poseImg"
        app:layout_constraintStart_toStartOf="@id/poseImg"
        app:layout_constraintEnd_toEndOf="@id/poseImg"
        android:hint="Pose Name"
        android:textSize="12sp"
        android:textColor="@android:color/white"
        android:maxLines="1"
        android:ellipsize="end"/>
</androidx.constraintlayout.widget.ConstraintLayout>