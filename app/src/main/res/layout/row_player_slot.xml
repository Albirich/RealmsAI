<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="4dp"
    android:gravity="center">

    <ImageView
        android:id="@+id/personaAvatar"
        android:layout_width="42dp"
        android:layout_height="42dp"
        app:layout_constraintStart_toStartOf="@id/personaName"
        app:layout_constraintEnd_toEndOf="@id/personaName"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/personaName"
        android:src="@drawable/icon_01"
        android:contentDescription="Persona"/>

    <TextView
        android:id="@+id/personaName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/personaAvatar"
        android:text="Select Persona"
        android:textStyle="bold"
        android:layout_marginStart="10dp"
        android:textSize="12sp"/>
</androidx.constraintlayout.widget.ConstraintLayout>
