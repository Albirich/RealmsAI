<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingTop="2dp"
    android:paddingBottom="2dp">

    <LinearLayout
        android:id="@+id/relationshipcontent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/RelationshipDivider">
    <TextView
        android:id="@+id/relationshipToNameEdit"
        android:layout_width="70dp"
        android:layout_height="wrap_content"
        android:hint="With (Name)"
        android:textSize="16sp"
        android:inputType="textPersonName|textMultiLine"/>

    <TextView
        android:id="@+id/relationshipType"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Type"
        android:textSize="16sp"
        android:layout_marginStart="6dp"/>
    <TextView
        android:id="@+id/relationshipSummaryEdit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Short desc"
        android:textSize="14sp"
        android:layout_marginStart="10dp"
        android:layout_weight="2"
        android:inputType="textCapSentences|textMultiLine"
        />


    <!-- Optionally, add a delete button -->
    <ImageButton
        android:id="@+id/btnDeleteRelationship"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:background="?android:selectableItemBackgroundBorderless"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_delete"
        android:contentDescription="Delete"
        android:layout_marginStart="4dp"/>
    </LinearLayout>
    <LinearLayout
        android:id="@+id/RelationshipDivider"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/relationshipcontent">
    <View
        android:id="@+id/relationshipSpacer"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="#000000"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp" />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
